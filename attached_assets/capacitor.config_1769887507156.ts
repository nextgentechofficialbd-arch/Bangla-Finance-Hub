import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shajid.khorochtracker',
  appName: 'Khoroch Tracker',
  webDir: 'dist'
};

export default config;
